export type OrganizationType = {
    id: number,
    name: string
    created_at: string,
    followers: number,
    is_verified: boolean,
    kinds_of_sports: string[],
    numer_of_leagues: number,
    numer_of_teams: number,
    region: string
}